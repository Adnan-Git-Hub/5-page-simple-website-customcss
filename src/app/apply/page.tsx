import '../styles/apply.css';
export default function Apply() {
    throw new Error(); // Keeps the error functionality intact
    return (
      <div>
        <h1 className="apply-title">Apply Page!</h1>
      </div>
    );
  }
  